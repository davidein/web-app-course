require(['platform'], function(Platform) {

  var BASELINE_WIDTH = 800;
  var BASELINE_VARIANCE = 500;
  var JUMP_DISTANCE_MIN = 150;
  var JUMP_DISTANCE_MAX = 150;

  
  var generate = function(length) {
    var curX = 0;
    var platforms = [];

    // Create baseline.
    var curY = BASELINE_START_Y;
    while (curX < length) {

    }
  };

  return {
    generate: generate
  };
});